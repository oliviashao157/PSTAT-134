# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
# Load the dataset
twitter <- read_csv("sample_50k.csv")
head(twitter)
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
head(twitter)
colSums(is.na(twitter))
colSums(is.na(twitter))
summary(twitter$sentiment)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment) 
# Check for duplicated rows
sum(duplicated(twitter))
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
head(twitter, n = 100)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of user
unique(twitter$user)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query) # We only have 1 level "NO_QUERY" so we can drop it
twitter_cleaned <- twitter %>% select(-query)
head(twitter_cleaned)
# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
library(lubridate)
library(hms)
twitter_cleaned <- twitter_cleaned %>%
# Save raw date strings before overwriting
mutate(date_raw = date) %>%
mutate(
# Parse the original datetime string
parsed_dt = parse_date_time(date_raw, orders = "a b d H:M:S z Y"),
# Extract and format date as MM/DD/YYYY, stored as Date type
date = as.Date(format(parsed_dt, "%m/%d/%Y"), format = "%m/%d/%Y"),
# Extract time and store as hms
time = as_hms(format(parsed_dt, "%H:%M:%S")),
# Extract timezone as character
time_zone = sub(".*\\s(\\S+)\\s\\d{4}$", "\\1", date_raw)
) %>%
# Drop helper columns
select(-parsed_dt, -date_raw)
head(twitter_cleaned)
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
head(twitter, n = 100)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query)
# We only have 1 level "NO_QUERY" so we can drop it
twitter_cleaned <- twitter %>% select(-query)
twitter_cleaned <- twitter_cleaned %>%
mutate(date_raw = date) %>%
mutate(
# Extract timezone before parsing
time_zone = sub(".*\\s(\\S+)\\s\\d{4}$", "\\1", date_raw),
# Strip timezone from string, then parse
parsed_dt = parse_date_time(
sub("(.*?)\\s+\\S+(\\s+\\d{4})$", "\\1\\2", date_raw),
orders = "a b d H:M:S Y"
),
# Extract and format date as MM/DD/YYYY, stored as Date type
date = as.Date(format(parsed_dt, "%m/%d/%Y"), format = "%m/%d/%Y"),
# Extract time and store as hms
time = as_hms(format(parsed_dt, "%H:%M:%S"))
) %>%
select(-parsed_dt, -date_raw)
head(twitter_cleaned)
unique(twitter$time_zone)
unique(twitter_cleaned$time_zone)
unique(twitter_cleaned$time_zone)
# I noticed that every time zone in the dataset is in PDT so I drop the column.
# And renamed the time column into time_pdt
twitter_cleaned <- twitter_cleaned %>% select(-time_zone)
twitter_cleaned <- twitter_cleaned %>%
rename(time_pdt = time)
head(twitter_cleaned)
# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
library(lubridate)
library(hms)
library(stringr)
library(tidytext)
# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
library(lubridate)
library(hms)
library(stringr)
install.packages("tidytext")
# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
library(lubridate)
library(hms)
library(stringr)
install.packages("tidytext")
# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
library(lubridate)
library(hms)
library(stringr)
library(tidytext)
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
head(twitter, n = 100)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query)
# We only have 1 level "NO_QUERY" so we can drop it
twitter_cleaned <- twitter %>% select(-query)
twitter_cleaned <- twitter_cleaned %>%
mutate(date_raw = date) %>%
mutate(
# Extract timezone before parsing
time_zone = sub(".*\\s(\\S+)\\s\\d{4}$", "\\1", date_raw),
# Strip timezone from string, then parse
parsed_dt = parse_date_time(
sub("(.*?)\\s+\\S+(\\s+\\d{4})$", "\\1\\2", date_raw),
orders = "a b d H:M:S Y"
),
# Extract and format date as MM/DD/YYYY, stored as Date type
date = as.Date(format(parsed_dt, "%m/%d/%Y"), format = "%m/%d/%Y"),
# Extract time and store as hms
time = as_hms(format(parsed_dt, "%H:%M:%S"))
) %>%
select(-parsed_dt, -date_raw)
head(twitter_cleaned)
unique(twitter_cleaned$time_zone)
# I noticed that every time zone in the dataset is in PDT so I drop the column.
# And renamed the time column into time_pdt
twitter_cleaned <- twitter_cleaned %>% select(-time_zone)
twitter_cleaned <- twitter_cleaned %>%
rename(time_pdt = time)
head(twitter_cleaned)
unique(twitter_cleaned$sentiment)
#Binorize sentiment
twitter_cleaned <- twitter_cleaned %>%
mutate(sentiment = factor(sentiment, levels = c(0, 1), labels = c("Negative", "Positive")))
head(twitter_cleaned)
# Clean up the text
twitter_cleaned <- twitter_cleaned %>%
mutate(text = str_to_lower(text),                          # lowercase
text = str_remove_all(text, "@\\w+"),               # remove @mentions
text = str_remove_all(text, "http\\S+|www\\S+"),    # remove URLs
text = str_remove_all(text, "[^a-z\\s]"),           # remove punctuation/numbers
text = str_squish(text))                            # remove extra whitespace
head(twitter_cleaned)
# Tokenize and remove stop words
twitter_cleaned <- twitter_cleaned %>%
unnest_tokens(word, text) %>%
anti_join(stop_words, by = "word")
head(twitter_cleaned)
head(twitter_cleaned, n = 100)
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
head(twitter, n = 100)
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query)
# We only have 1 level "NO_QUERY" so we can drop it
twitter_cleaned <- twitter %>% select(-query)
twitter_cleaned <- twitter_cleaned %>%
mutate(date_raw = date) %>%
mutate(
# Extract timezone before parsing
time_zone = sub(".*\\s(\\S+)\\s\\d{4}$", "\\1", date_raw),
# Strip timezone from string, then parse
parsed_dt = parse_date_time(
sub("(.*?)\\s+\\S+(\\s+\\d{4})$", "\\1\\2", date_raw),
orders = "a b d H:M:S Y"
),
# Extract and format date as MM/DD/YYYY, stored as Date type
date = as.Date(format(parsed_dt, "%m/%d/%Y"), format = "%m/%d/%Y"),
# Extract time and store as hms
time = as_hms(format(parsed_dt, "%H:%M:%S"))
) %>%
select(-parsed_dt, -date_raw)
head(twitter_cleaned)
unique(twitter_cleaned$time_zone)
# I noticed that every time zone in the dataset is in PDT so I drop the column.
# And renamed the time column into time_pdt
twitter_cleaned <- twitter_cleaned %>% select(-time_zone)
twitter_cleaned <- twitter_cleaned %>%
rename(time_pdt = time)
head(twitter_cleaned)
#Binorize sentiment
twitter_cleaned <- twitter_cleaned %>%
mutate(sentiment = factor(sentiment, levels = c(0, 1), labels = c("Negative", "Positive")))
# Clean up the text
twitter_cleaned <- twitter_cleaned %>%
mutate(text = str_to_lower(text),                          # lowercase
text = str_remove_all(text, "@\\w+"),               # remove @mentions
text = str_remove_all(text, "http\\S+|www\\S+"),    # remove URLs
text = str_remove_all(text, "[^a-z\\s]"),           # remove punctuation/numbers
text = str_squish(text))                            # remove extra whitespace
# Tokenize and remove stop words
twitter_cleaned <- twitter_cleaned %>%
unnest_tokens(word, text) %>%
anti_join(stop_words, by = "word")
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query)
# We only have 1 level "NO_QUERY" so we can drop it
twitter_cleaned <- twitter %>% select(-query)
# Split date column into date, time, and time_zone
twitter_cleaned <- twitter_cleaned %>%
mutate(date_raw = date) %>%
mutate(
# Extract timezone before parsing
time_zone = sub(".*\\s(\\S+)\\s\\d{4}$", "\\1", date_raw),
# Strip timezone from string, then parse
parsed_dt = parse_date_time(
sub("(.*?)\\s+\\S+(\\s+\\d{4})$", "\\1\\2", date_raw),
orders = "a b d H:M:S Y"
),
# Extract and format date as MM/DD/YYYY, stored as Date type
date = as.Date(format(parsed_dt, "%m/%d/%Y"), format = "%m/%d/%Y"),
# Extract time and store as hms
time = as_hms(format(parsed_dt, "%H:%M:%S"))
) %>%
select(-parsed_dt, -date_raw)
unique(twitter_cleaned$time_zone)
# I noticed that every time zone in the dataset is in PDT so I drop the column.
# And renamed the time column into time_pdt
twitter_cleaned <- twitter_cleaned %>% select(-time_zone)
twitter_cleaned <- twitter_cleaned %>%
rename(time_pdt = time)
# Binarize sentiment (0 = negative, 1 = positive)
twitter_cleaned <- twitter_cleaned %>%
mutate(sentiment = factor(sentiment, levels = c(0, 1), labels = c("Negative", "Positive")))
# Clean up the text
twitter_cleaned <- twitter_cleaned %>%
mutate(text = str_to_lower(text),                          # lowercase
text = str_remove_all(text, "@\\w+"),               # remove @mentions
text = str_remove_all(text, "http\\S+|www\\S+"),    # remove URLs
text = str_remove_all(text, "[^a-z\\s]"),           # remove punctuation/numbers
text = str_squish(text))                            # remove extra whitespace
# Tokenize and remove stop words
twitter_cleaned <- twitter_cleaned %>%
unnest_tokens(word, text) %>%
anti_join(stop_words, by = "word")
head(twitter_cleaned, n = 10)
# knit options
knitr::opts_chunk$set(echo = T,
results = 'markup',
fig.width = 4,
fig.height = 3,
fig.align = 'center',
message = F,
warning = F)
# packages
library(tidyverse)
library(faraway)
library(data.table)
library(dplyr)
library(tidymodels)
library(lubridate)
library(hms)
library(stringr)
library(tidytext)
# load the dataset
twitter <- read_csv("sample_50k.csv")
# adding back the column names
colnames(twitter) <- c("sentiment", "id", "date", "query", "user", "text")
# Check for missing values
colSums(is.na(twitter))
# Check summary stats for sentiment
summary(twitter$sentiment)
# Check for duplicated rows
sum(duplicated(twitter))
# View levels of query
unique(twitter$query)
# We only have 1 level "NO_QUERY" so we can drop it
twitter_cleaned <- twitter %>% select(-query)
# Split date column into date, time, and time_zone
twitter_cleaned <- twitter_cleaned %>%
mutate(date_raw = date) %>%
mutate(
# Extract timezone before parsing
time_zone = sub(".*\\s(\\S+)\\s\\d{4}$", "\\1", date_raw),
# Strip timezone from string, then parse
parsed_dt = parse_date_time(
sub("(.*?)\\s+\\S+(\\s+\\d{4})$", "\\1\\2", date_raw),
orders = "a b d H:M:S Y"
),
# Extract and format date as MM/DD/YYYY, stored as Date type
date = as.Date(format(parsed_dt, "%m/%d/%Y"), format = "%m/%d/%Y"),
# Extract time and store as hms
time = as_hms(format(parsed_dt, "%H:%M:%S"))
) %>%
select(-parsed_dt, -date_raw)
unique(twitter_cleaned$time_zone)
# I noticed that every time zone in the dataset is in PDT so I drop the column.
# And renamed the time column into time_pdt
twitter_cleaned <- twitter_cleaned %>% select(-time_zone)
twitter_cleaned <- twitter_cleaned %>%
rename(time_pdt = time)
# Binarize sentiment (0 = negative, 1 = positive)
twitter_cleaned <- twitter_cleaned %>%
mutate(sentiment = factor(sentiment, levels = c(0, 1), labels = c("Negative", "Positive")))
# Clean up the text
twitter_cleaned <- twitter_cleaned %>%
mutate(text = str_to_lower(text),                          # lowercase
text = str_remove_all(text, "@\\w+"),               # remove @mentions
text = str_remove_all(text, "http\\S+|www\\S+"),    # remove URLs
text = str_remove_all(text, "[^a-z\\s]"),           # remove punctuation/numbers
text = str_squish(text))                            # remove extra whitespace
# Tokenize and remove stop words
twitter_cleaned <- twitter_cleaned %>%
unnest_tokens(word, text) %>%
anti_join(stop_words, by = "word")
head(twitter_cleaned, n = 10)
View(twitter_cleaned)
